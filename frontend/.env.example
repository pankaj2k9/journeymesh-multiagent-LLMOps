# JourneyMesh frontend environment template.
# Only VITE_* variables reach the browser - never put a secret here.

# Base URL of the JourneyMesh API, including the version prefix.
# Leave empty in development to use the Vite dev-server proxy.
VITE_API_BASE_URL=
